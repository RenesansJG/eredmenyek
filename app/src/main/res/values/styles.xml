<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:textColorHint">@color/primary_text</item>
    </style>

    <style name="ItemRow">
        <item name="rowHeight">@dimen/item_row_height</item>
        <item name="rowPadding">@dimen/item_row_padding</item>
        <item name="imageWidth">@dimen/item_image_width</item>
        <item name="imageHeight">@dimen/item_image_height</item>
        <item name="textMarginLeft">@dimen/item_text_margin_left</item>
        <item name="android:background">@drawable/row_background</item>
        <item name="android:textSize">@dimen/item_text_size</item>
        <item name="android:textColor">@color/primary_text</item>
    </style>

    <style name="CategoryRow">
        <item name="rowHeight">@dimen/category_row_height</item>
        <item name="rowPadding">@dimen/category_row_padding</item>
        <item name="imageWidth">@dimen/category_image_width</item>
        <item name="imageHeight">@dimen/category_image_height</item>
        <item name="textMarginLeft">@dimen/category_text_margin_left</item>
        <item name="android:background">@color/accent</item>
        <item name="android:textSize">@dimen/category_text_size</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textAllCaps">true</item>
    </style>

</resources>
